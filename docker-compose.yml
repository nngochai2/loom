services:
  neo4j:
    image: neo4j:5-community
    ports:
      - "7474:7474" # browser UI
      - "7687:7687" # bolt
    environment:
      NEO4J_AUTH: neo4j/loom-dev-password
      NEO4J_PLUGINS: "[]"
    volumes:
      - neo4j-data:/data

volumes:
  neo4j-data:
