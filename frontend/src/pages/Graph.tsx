/** Placeholder — the real NVL-canvas Graph page (spec §9) is issue #13. */
export function Graph() {
  return (
    <div className="p-8 text-neutral-500">
      <h1 className="text-lg font-semibold text-neutral-900">Graph</h1>
      <p className="mt-2 text-sm">Coming soon (#13).</p>
    </div>
  );
}
