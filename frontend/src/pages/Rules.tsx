/** Placeholder — the real two-pane Rules page (spec §9, ADR-0024) is issue #11. */
export function Rules() {
  return (
    <div className="p-8 text-neutral-500">
      <h1 className="text-lg font-semibold text-neutral-900">Rules</h1>
      <p className="mt-2 text-sm">Coming soon (#11).</p>
    </div>
  );
}
